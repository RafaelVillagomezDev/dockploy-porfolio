User-agent: *

# BLOQUEOS ESENCIALES: Directorios que contienen código fuente o secretos
Disallow: /api/
Disallow: /tmp/
Disallow: /node_modules/
Disallow: /src/
Disallow: /.github/
Disallow: /.nginx/
Disallow: /etc/
Disallow: /build/   # Bloquea carpeta común de build
Disallow: /dist/    # Bloquea carpeta común de build (aunque la necesites, la línea 'Allow: /' abajo lo permite si está en la raíz)

# BLOQUEOS DE ARCHIVOS DE CONFIGURACIÓN SENSIBLES
Disallow: /*.env$
Disallow: /*.json$
Disallow: /*.babelrc$

# -------------------------------------------------------------
# REGLAS DE ACCESO
# -------------------------------------------------------------

# Permitir el acceso explícito al subdirectorio donde está la SPA
Allow: /porfolio/

# Permitir el acceso a todos los demás archivos y assets de la raíz
# Esto anula parcialmente los bloqueos anteriores para archivos esenciales
# (Ej: /main.js si está en la raíz, pero protege /api/)
Allow: /

# UBICACIÓN DEL SITEMAP (¡CORREGIDO!)
Sitemap: https://yandrydev.cloud/sitemap.xml